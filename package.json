{
  "name": "exam-management-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "db:setup": "npm run db:migrate && npm run seed",
    "db:migrate": "npm run prisma:migrate --prefix backend",
    "seed": "npm run seed --prefix backend",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:all": "npm run prisma:generate --prefix backend && npm run build",
    "package:release": "npm run build:all && node deploy/package-release.mjs",
    "desktop:install": "npm install --prefix desktop",
    "desktop:dev": "npm run dev --prefix desktop",
    "desktop:build:exe": "npm run build:all && npm run dist:win --prefix desktop",
    "desktop:build:linux": "npm run build:all && npm run dist:linux --prefix desktop",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:build": "docker compose build",
    "docs:pdf": "node scripts/generate-pdf-guide.mjs",
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run start:prod --prefix backend\" \"npm run start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
